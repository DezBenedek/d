# d
The ultimate CLI for MacOS

commands:
cargo build --release
./build-pkg.sh
gh release create v0.3.0 target/release/d docs/d-installer.pkg --notes "v0.3.0 added git fix, update and new updater system"
